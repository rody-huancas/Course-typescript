// para definir el tipo de varible, se le asigna después del nombre de la variable

// varible numerica
let numero: number = 20;
// numero = "prueba"; -> al ser de tipo numerico, no se puede pasar a un string, a diferencia de como se hace con js

// variable de tipo string
let cadena: string = "Hola Mundo";

// variable de tipo boolean
let bandera: boolean = false;

console.log(bandera);
